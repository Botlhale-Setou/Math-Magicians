import React from 'react';
import Navbar from './navbar';
import './components.css';

const Quote = () => (
  <div>
    <Navbar />
    <h1 className="quoteBody">Math - the only place where one can buy 64 watermelons and no one wonders why!</h1>
  </div>
);

export default Quote;
